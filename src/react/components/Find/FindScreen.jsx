import React, { useEffect, useState } from 'react';
import {
    TransitionablePortal,
    Segment,
    Divider,
    Header,
    Icon,
    Button,
} from 'semantic-ui-react';
import { connect } from 'react-redux';

const FindScreen = (props) => {
    return <div>FindScreen</div>;
};

export default FindScreen;
